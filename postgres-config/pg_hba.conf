# TYPE  DATABASE        USER            ADDRESS                 METHOD
#####################################################################
## CONFIGURATION mTLS (Mutual TLS) - AUTHENTIFICATION PAR CERTIFICAT
## Toutes les connexions TCP/IP requièrent un certificat client valide
##
## MAPPING DE CERTIFICATS: Utilise pg_ident.conf pour mapper les CN
## des certificats vers les utilisateurs PostgreSQL. Cela permet
## d'avoir plusieurs certificats (1 par poste) pour le même utilisateur.
#####################################################################

# 1. Connexion locale par socket Unix (pas de SSL nécessaire)
# Authentification par mot de passe sécurisé
local   all             all                                     scram-sha-256

#####################################################################
## CONNEXIONS TCP/IP - mTLS OBLIGATOIRE AVEC MAPPING                ##
#####################################################################

# 2. Connexions localhost via TCP/IP avec mTLS + mapping
# 'cert' = authentification par certificat uniquement
# 'map=cert_map' = utilise le mapping défini dans pg_ident.conf
hostssl all             all             127.0.0.1/32            cert clientcert=verify-full map=cert_map
hostssl all             all             ::1/128                 cert clientcert=verify-full map=cert_map

# 3. Connexions DISTANTES avec mTLS OBLIGATOIRE + mapping
# Le client DOIT présenter un certificat valide signé par la CA
# Le CN du certificat est mappé vers l'utilisateur PostgreSQL via pg_ident.conf
hostssl all             all             0.0.0.0/0               cert clientcert=verify-full map=cert_map
hostssl all             all             ::/0                    cert clientcert=verify-full map=cert_map

#####################################################################
## RÈGLES DE RÉPLICATION - mTLS OBLIGATOIRE                         ##
#####################################################################

local   replication     all                                     scram-sha-256
hostssl replication     all             127.0.0.1/32            cert clientcert=verify-full
hostssl replication     all             ::1/128                 cert clientcert=verify-full
hostssl replication     all             0.0.0.0/0               cert clientcert=verify-full
hostssl replication     all             ::/0                    cert clientcert=verify-full

#####################################################################
## SÉCURITÉ RENFORCÉE                                               ##
#####################################################################
# AUCUNE connexion 'host' (non-SSL) n'est autorisée
# AUCUNE connexion 'trust' n'est autorisée
# Toutes les connexions réseau doivent utiliser mTLS